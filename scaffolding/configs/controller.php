	return iw::createConfig('Controller', array(
		//
		// The controller can have custom routes which are modular to the controller.  These routes
		// do not need to be added independently to the global routes config, and are good for
		// common re-usable controllers or subsections.
		//
		'routes' => array(

		)
	));
